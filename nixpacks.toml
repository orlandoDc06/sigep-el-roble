[phases.setup]
nixPkgs = ['supervisor', 'procps']

[phases.install]
cmds = ['composer install --no-dev --optimize-autoloader']

[phases.build]
cmds = ['npm install', 'npm run build']

[start]
cmd = "supervisord -c supervisord.conf && php artisan serve --host=0.0.0.0 --port=$PORT"